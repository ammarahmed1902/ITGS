# Server
API_PORT=3001
ADMIN_API_KEY="change-me-in-production"

# Unified data directory for contact + job application JSON stores
ITGS_DATA_DIR=./data

# Legacy alias (still supported)
# ITGS_CONTACT_DATA_DIR=./data

# Frontend (Vite) — dev only for admin applications API access
# In production, replace with proper server-side auth
VITE_ADMIN_API_KEY="change-me-in-production"

# Calendly embed URL
VITE_CALENDLY_URL="https://calendly.com/ammarzerobyte/30min"
